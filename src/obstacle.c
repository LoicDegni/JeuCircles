#include "obstacle.h"


//struct obstacle  obstacle_initialize(int rayon) 
//{
//   struct obstacle nouvelle_obstacle;
//   nouvelle_obstacle.rayon = RAYON_MAX;
//   return nouvelle_obstacle;
//}

// A completer
//int obstacle_display(SDL_Renderer *renderer, struct obstacle *nouvelle_obstacle) {
//   if(renderer == NULL){
//      fprintf(stderr, "Pointeur invalide");
//      exit(1);
//   }
//   if(nouvelle_obstacle == NULL) {
//      fprintf(stderr, "Pointeur invalide");
//      exit(1);
//   }
//   srand(time(NULL));
//   int fin_cadran = ((rand() % 6) +1)*60 - 1;
//   int debut_cadran = fin_cadran - 59;
//   thickArcRGBA(renderer, CENTER_X, CENTER_Y, nouvelle_obstacle->rayon, debut_cadran, fin_cadran, 5, 0, 0, 255, 255);
//   return 0;
//}


